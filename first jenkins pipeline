node{
      for (i=0; i<2; i++) 
      { 
           stage "Stage #"+i
           print 'Hello, world !'
         {
              if (i = 0; i++)
              echo ("Sucess")
              else 
              build ("Declarative pipeline")
              echo ("Failure")
         }
      }
    }  
